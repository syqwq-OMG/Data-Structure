#include <iostream>
#include <vector>
#define rep(i, a, b) for (int i = a; i <= b; i++)
#define per(i, a, b) for (int i = a; i >= b; i--)
using namespace std;

const int N = 1e3 + 5;

int n, m;
int k;
vector<int> a(N), b(N);

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    // -1 3 4 2 -2 -1 999 -998
    return 0;
}